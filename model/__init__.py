# BreastCancerAI — baseline models (weights/outputs are gitignored)
# Checkpoints under model/baselines/ are reproducible artifacts (seed 20260918)
# and are NOT tracked; .gitignore already excludes *.pt / *.pth.
